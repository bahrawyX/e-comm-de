# Hero image

Save the blue headphones render here as **`hero-headphones.png`**.

Anything in this `public/` folder is served from the site root, so the file is
referenced in the app as `/hero-headphones.png` (see the hero in
`src/pages/Home.tsx`). Until the file exists, the hero falls back to the first
product image.
