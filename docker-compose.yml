services:
  db:
    image: postgres:17
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Dih@1234
      POSTGRES_DB: e_comm
      POSTGRES_HOST_AUTH_METHOD: trust
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  # backend:
  #   build:
  #     context: api
  #     dockerfile: Dockerfile
  #   environment:
  #     DB_URL: jdbc:postgresql://db:5432/e_comm
  #     DB_USERNAME: postgres
  #     DB_PASSWORD: Dih@1234
  #   depends_on:
  #     - db
  #   ports:
  #     - "8080:8080"
  #   volumes:
  #     - .api/.env:/app/.env

  # frontend:
  #   build:
  #     context: webapp
  #     dockerfile: Dockerfile
  #   ports:
  #     - "5173:5173"
  #   depends_on:
  #     - backend

volumes:
  postgres_data: